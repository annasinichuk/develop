#include <stdio.h>
int max(int numeral, int numeral1) {
    if (numeral > numeral1)
        printf("%d", numeral);
    if (numeral1 > numeral)
        printf("%d", numeral1);
    if (numeral == numeral1)
        printf("n/a");
    return 0;
}
int main() {
    int numeral, numeral1;
    char symbol;
    if (scanf("%d %d%c", &numeral, &numeral1, &symbol) != 3 || symbol != '\n') {
        printf("n/a");
    } else {
       max(numeral, numeral1)
    }
        return 0;
        }
